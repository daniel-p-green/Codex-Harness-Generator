model = "gpt-5.5"
model_reasoning_effort = "high"
model_verbosity = "medium"
approval_policy = "on-request"
default_permissions = "security-audit"

[agents]
max_threads = 4
max_depth = 1

[agents.reviewer]
description = "Reviews security-audit outputs for exploitability, evidence, and privacy risk."
config_file = "agents/reviewer.toml"

[[skills.config]]
path = "../.agents/skills/health-check"
enabled = true

[permissions.security-audit]
description = "Workspace write access with sensitive files denied."
extends = ":workspace"

[permissions.security-audit.filesystem]
glob_scan_max_depth = 4

[permissions.security-audit.filesystem.":workspace_roots"]
"." = "write"
"**/.env" = "deny"
"**/.env.*" = "deny"
"**/*secret*" = "deny"
"**/*token*" = "deny"
"**/*credential*" = "deny"
"**/*.pem" = "deny"
"**/*.key" = "deny"

[permissions.security-audit.network]
enabled = true
mode = "limited"

[permissions.security-audit.network.domains]
"developers.openai.com" = "allow"
"docs.github.com" = "allow"
