model = "gpt-5.5"
model_reasoning_effort = "medium"
model_verbosity = "medium"
approval_policy = "on-request"
default_permissions = "data-engineering"

[agents]
max_threads = 4
max_depth = 1

[agents.reviewer]
description = "Reviews data engineering outputs for source fidelity, correctness, privacy, safety, and missing verification."
config_file = "agents/reviewer.toml"

[[skills.config]]
path = "../.agents/skills/health-check"
enabled = true

[permissions.data-engineering]
description = "Workspace write access with sensitive files denied."
extends = ":workspace"

[permissions.data-engineering.filesystem]
glob_scan_max_depth = 4

[permissions.data-engineering.filesystem.":workspace_roots"]
"." = "write"
"**/.env" = "deny"
"**/.env.*" = "deny"
"**/*secret*" = "deny"
"**/*token*" = "deny"
"**/*credential*" = "deny"
"**/*.pem" = "deny"
"**/*.key" = "deny"

[permissions.data-engineering.network]
enabled = true
mode = "limited"

[permissions.data-engineering.network.domains]
"developers.openai.com" = "allow"
"docs.github.com" = "allow"
