# Proof Matrix

Generated: 2026-06-04

This matrix ties public claims to files and commands that can be inspected in
this repository. It is intentionally conservative: passing evidence below proves
the stated scope, not more.

## Claims And Evidence

| Claim | Evidence artifact | Verification command | Proven scope |
|---|---|---|---|
| The repo is structurally Codex-native. | `AGENTS.md`, `.codex/config.toml`, `.codex/agents/*.toml`, `.agents/skills/*/SKILL.md`, `Docs/Environment/CODEX_PORT_EVALUATION.md` | `python scripts/eval_codex_port.py` | Root generator files use Codex paths, TOML schemas, permission profiles, and official-source citations without legacy platform residue. |
| Codex-native equivalence is mapped and tested. | `scripts/check_codex_equivalence.py`, `Docs/Environment/CODEX_EQUIVALENCE_MATRIX.md`, `tests/test_codex_equivalence.py`, `tests/test_codex_harness_cli.py` | `python scripts/codex_harness.py equivalence --no-write --json && python -m unittest tests.test_codex_equivalence tests.test_codex_harness_cli -q` | The repo maps the earlier harness-generator responsibilities to Codex-native instruction, config, subagent, skill, profile, generation, validation, adoption, copied-harness, guardrail, usage-evidence, and release-proof surfaces without claiming external adoption. |
| Source lineage is auditable against the source upstream ref. | `scripts/check_upstream_drift.py`, `Docs/Environment/UPSTREAM_DRIFT.md`, `tests/test_upstream_drift.py`, `tests/test_codex_harness_cli.py` | `python scripts/codex_harness.py upstream-drift --no-write --json && python -m unittest tests.test_upstream_drift tests.test_codex_harness_cli -q` | Maintainers can verify whether the source upstream ref has upstream-only commits requiring Codex-port review, while keeping the claim boundary explicit: source-drift status does not prove semantic equivalence, external adoption, or production readiness. |
| Generated harness fixtures satisfy the local contract. | `tests/fixtures/generated_harnesses/` | `python scripts/eval_generated_harness.py tests/fixtures/generated_harnesses/*` | Representative generated harnesses pass required-path, config, agent, skill, source-map, manifest, assumptions, eval-plan, improvement-log, and safety checks. |
| Generated harnesses can be read and resolved offline. | `tests/fixtures/generated_harnesses/`, `examples/deterministic/`, `examples/create-acceptance/`, `examples/brief-acceptance/` | `python scripts/smoke_generated_harness.py <harness-paths>` | Codex-facing files parse, referenced agents and skills resolve, and smoke checks do not require live auth. |
| Generated harnesses include a project eval plan. | `scripts/generate_minimal_harness.py`, `scripts/eval_generated_harness.py`, `tests/test_generated_harness_contract.py`, generated `Docs/Environment/EVAL_PLAN.md` files | `python scripts/eval_generated_harness.py examples/deterministic/software-development` | Generated harnesses carry portable success criteria, smoke checks, acceptance checks, reviewer checks, and regression checks so copied harnesses define what useful work should prove, not only that files are present. |
| Generated harnesses include a self-improvement loop. | `scripts/generate_minimal_harness.py`, `scripts/eval_generated_harness.py`, `tests/test_generated_harness_contract.py`, generated `Docs/Environment/IMPROVEMENT_LOG.md`, `scripts/record-improvement.py`, and `scripts/summarize-improvements.py` files | `tmp=$(mktemp -d) && cp -R examples/deterministic/software-development "$tmp/harness" && python "$tmp/harness/scripts/record-improvement.py" --category CHECK_GAP --task "demo" --friction "missing command" --evidence "public-safe fixture" --candidate-update "Add command to eval plan" --verification "python scripts/check-harness.py" --status proposed && python "$tmp/harness/scripts/summarize-improvements.py" --json` | Generated harnesses carry categories, seed patterns, entry templates, update rules, a local recorder, and a local backlog summary/check for converting repeated friction and user corrections into small verified harness updates. |
| Generated harnesses include a copied-harness task-trial lane. | `scripts/generate_minimal_harness.py`, `scripts/eval_generated_harness.py`, `tests/test_generated_harness_contract.py`, generated `Docs/Environment/TASK_TRIALS.md`, `scripts/record-task-trial.py`, and `scripts/summarize-task-trials.py` files | `tmp=$(mktemp -d) && cp -R examples/deterministic/software-development "$tmp/harness" && python "$tmp/harness/scripts/record-task-trial.py" --task "demo" --outcome success --evidence "public-safe artifact" --verification "python scripts/check-harness.py" --privacy-review "public-safe summary only" --limitations "one copied-harness smoke" && python "$tmp/harness/scripts/summarize-task-trials.py" --min-successes 1` | Copied harnesses can record and summarize task outcome, evidence, verification, privacy review, how the harness helped, and limitations without relying on this generator repo. |
| Generated harnesses include a copied-harness eval report. | `scripts/generate_minimal_harness.py`, `scripts/eval_generated_harness.py`, `tests/test_generated_harness_contract.py`, generated `scripts/run-harness-evals.py` and `Docs/Environment/EVAL_REPORT.md` files | `tmp=$(mktemp -d) && cp -R examples/deterministic/software-development "$tmp/harness" && python "$tmp/harness/scripts/run-harness-evals.py" --json` | Copied harnesses can run their local smoke check, summarize task-trial evidence, summarize improvement backlog state, and write `Docs/Environment/EVAL_REPORT.md` without relying on this generator repo. |
| Generated harnesses can draft public-safe usage reports locally. | `scripts/generate_minimal_harness.py`, generated `scripts/export-public-usage-report.py`, generated `Docs/Environment/TASK_TRIALS.md`, `tests/test_generated_harness_contract.py` | `tmp=$(mktemp -d) && cp -R examples/deterministic/software-development "$tmp/harness" && python "$tmp/harness/scripts/record-task-trial.py" --task "demo" --outcome success --evidence "public-safe artifact" --verification "python scripts/check-harness.py" --privacy-review "public-safe summary only" --limitations "one copied-harness smoke" && python "$tmp/harness/scripts/export-public-usage-report.py" --out "$tmp/harness/Docs/Environment/PUBLIC_USAGE_REPORT.md" --json` | Reporters can turn the latest complete successful copied-harness task trial plus local eval status into the same public-safe issue-body sections consumed by `codex-harness usage-from-issue`, without requiring access to this generator repo or exposing raw private evidence. |
| Generated harnesses include a local self-check. | `scripts/generate_minimal_harness.py`, `scripts/check_example_inventory.py`, `tests/test_generated_harness_contract.py`, generated `scripts/check-harness.py` files | `python tests/fixtures/generated_harnesses/software-dev-basic/scripts/check-harness.py` | Generated harnesses carry a dependency-light local smoke check so users can verify required paths, manifest references, agent/skill config targets, eval-plan content, improvement-log content, and eval-report sections after copying a harness away from this generator repo. |
| Generated harnesses guide a first useful task. | `scripts/generate_minimal_harness.py`, generated `Docs/GETTING_STARTED.md`, generated `scripts/check-harness.py`, `tests/test_generated_harness_contract.py` | `python -m unittest tests.test_generated_harness_contract -q` | Generated harnesses now give users a profile-specific first-task loop, verification menu, task-trial recording command, local eval command, local public-report export command, and privacy-safe reporting boundary so setup can turn into usable evidence. |
| The deterministic profile generator covers 20 first-class starting points. | `scripts/generate_minimal_harness.py`, `scripts/eval_deterministic_profiles.py`, `examples/deterministic/` | `python scripts/eval_deterministic_profiles.py` | The four base starter profiles and 16 bundled domain presets each produce a valid minimal harness. |
| Supported profiles are discoverable before generation. | `scripts/profile_catalog.py`, `scripts/codex_harness.py`, `tests/test_profile_catalog.py`, `tests/test_codex_harness_cli.py` | `python scripts/codex_harness.py profiles --details && python scripts/codex_harness.py profile security-audit && python scripts/codex_harness.py recommend "RAG app with prompts and evals"` | Users can inspect profile domains, targets, first tasks, permission profile names, reviewer focus, high-risk guardrails, explainable brief-based recommendations, confidence labels, and low-confidence guidance before choosing a deterministic starter. |
| Existing project metadata can guide profile selection. | `scripts/inspect_project.py`, `scripts/codex_harness.py`, `tests/test_inspect_project.py`, `tests/test_codex_harness_cli.py` | `python scripts/codex_harness.py inspect . --json && python -m unittest tests.test_inspect_project -q` | Users can point the helper at a local project and get metadata-derived starter recommendations, detected config files, top-level directory signals, extension counts, and a next `codex-harness init` command without publishing source contents or absolute local paths. |
| Existing project metadata can drive generation. | `scripts/run_inspected_acceptance.py`, `scripts/inspect_project.py`, `scripts/codex_harness.py`, `tests/test_inspected_acceptance.py`, `tests/test_codex_harness_cli.py` | `tmp=$(mktemp -d) && mkdir -p "$tmp/source/prompts" "$tmp/source/src" && printf '[project]\nname = "demo"\n' > "$tmp/source/pyproject.toml" && printf 'Use sources.\n' > "$tmp/source/prompts/answer.md" && printf 'def search():\n    return []\n' > "$tmp/source/src/retrieval.py" && python scripts/codex_harness.py init "$tmp/harness" --from-project "$tmp/source" --source-label "demo source" --force --json` | Users can generate a validated harness from local project metadata in one command, preserving an auditable `Docs/Environment/PROJECT_INSPECTION.md` report without copying source contents. |
| Existing projects have a non-destructive adoption preview and add-only copy bridge. | `scripts/plan_project_adoption.py`, `scripts/codex_harness.py`, `tests/test_plan_project_adoption.py`, `tests/test_codex_harness_cli.py` | `tmp=$(mktemp -d) && mkdir -p "$tmp/source/src" && printf '# Existing\n' > "$tmp/source/AGENTS.md" && printf '[project]\nname = "demo"\n' > "$tmp/source/pyproject.toml" && python scripts/codex_harness.py adoption-plan "$tmp/source" --source-label "demo source" --report "$tmp/ADOPTION_PLAN.md" --blueprint-out "$tmp/blueprint" --copy-script "$tmp/copy-adds.sh" --json && "$tmp/copy-adds.sh"` | Users can preview which generated harness files would be added, conflict, or already match, then run an optional script that copies only add rows and refuses to overwrite existing project files; conflict rows remain manual, the default plan payload avoids source contents and implicit absolute project paths, and the report/script now surface post-adoption checks for generated self-check, copied local eval, and task-trial recording. |
| A short brief can drive a deterministic harness acceptance run. | `scripts/run_brief_acceptance.py`, `scripts/refresh_brief_acceptance_examples.py`, `examples/brief-acceptance/`, `tests/test_brief_acceptance.py`, `tests/test_brief_acceptance_examples.py` | `python scripts/run_brief_acceptance.py /tmp/codex-rag-harness --brief "RAG app with prompts and evals" --force` | The fast path can recommend a profile from a brief, run trigger handoff, generate a harness, eval and smoke it, and record `Docs/Environment/PROFILE_SELECTION.md` with the selection rationale; checked-in snapshots cover RAG, security, support, and hiring briefs. |
| First-run quickstart works as one command. | `scripts/run_quickstart.py`, `scripts/codex_harness.py`, `tests/test_quickstart.py`, `tests/test_codex_harness_cli.py` | `python scripts/codex_harness.py quickstart /tmp/codex-rag-harness --brief "RAG app with prompts, evals, and retrieval checks" --force --json && python -m unittest tests.test_quickstart tests.test_codex_harness_cli -q` | Users can generate a harness from a brief, validate it, run its copied local eval, and inspect `Docs/Environment/QUICKSTART_REPORT.md` in one command; this proves local first-run mechanics, not external adoption or production readiness. |
| The public demo path is reproducible and inspectable. | `scripts/run_demo_capture.py`, `examples/demo-capture/`, `tests/test_demo_capture.py` | `python scripts/run_demo_capture.py /tmp/codex-demo-harness --brief "RAG app with prompts, evals, and retrieval checks" --force` | Maintainers can generate a public-safe demo harness, inspect `PROFILE_SELECTION.md` and `AGENTS.md`, run validation, and review `Docs/Environment/DEMO_CAPTURE.md` without relying on private data or external adoption claims. |
| Checked-in deterministic examples are reproducible. | `scripts/refresh_deterministic_examples.py`, `examples/deterministic/` | `python scripts/refresh_deterministic_examples.py && python scripts/run_evals.py` | Example snapshots for all 20 first-class starting points can be regenerated and are evaluated and smoke-checked by the release gate. |
| The `/create` trigger handoff is contract-tested. | `scripts/simulate_create_trigger.py`, `tests/test_create_trigger_contract.py` | `pytest -q tests/test_create_trigger_contract.py` | Fresh targets, existing environments, hub add-area detection, and interrupted-generation resume write the expected `CREATION_CONTEXT.md` shape. |
| The deterministic preset `/create` acceptance flow works as one target-level artifact. | `scripts/run_create_acceptance.py`, `examples/create-acceptance/*/Docs/Environment/CREATE_ACCEPTANCE_REPORT.md` | `python scripts/run_create_acceptance.py /tmp/codex-create-acceptance --profile software-development --force` | Trigger context, preset harness generation, eval, smoke, manifest, and acceptance report work together in one directory. |
| Checked-in create-acceptance examples cover every deterministic preset. | `examples/create-acceptance/` | `python scripts/eval_generated_harness.py examples/create-acceptance/* && python scripts/smoke_generated_harness.py examples/create-acceptance/*` | Each base profile and bundled domain preset has an inspectable trigger-plus-generated-harness snapshot with `CREATION_CONTEXT.md` and `CREATE_ACCEPTANCE_REPORT.md`. |
| Live `/create` outputs have a repeatable capture lane. | `scripts/capture_live_create_example.py`, `examples/live-create/README.md`, `tests/test_live_create_capture.py` | `python -m unittest tests.test_live_create_capture -q` | Maintainers can run or import a live generated target, require `CREATION_CONTEXT.md`, eval and smoke it, strip common unsafe files, and write a capture report before checking in examples. |
| Checked-in live-example artifacts cover multiple synthetic project shapes. | `examples/live-create/INDEX.md`, `examples/live-create/synthetic-*` | `python scripts/eval_generated_harness.py examples/live-create/synthetic-* && python scripts/smoke_generated_harness.py examples/live-create/synthetic-*` | Knowledge-work, Python CLI, and data-analysis live captures plus public-safe security-audit, legal-research, financial-modeling, hiring-pipeline, and customer-support task-trial fixtures include trigger context, capture notes, source map, assumptions, eval pass, and smoke pass. |
| Generated harnesses can steer Codex through representative tasks. | `scripts/run_live_example_task_trials.py`, `examples/live-create/TASK_TRIALS.md`, `tests/test_live_example_task_trials.py` | `python scripts/run_live_example_task_trials.py` | Authenticated Codex runs against temporary copies of checked-in live examples seed synthetic inputs and verify concrete output files for notes, Python CLI TODO audit, data review, defensive security review, source-backed legal research, financial scenario analysis, structured hiring support, and grounded customer-support escalation workflows. |
| High-risk generated harnesses require explicit domain guardrails. | `scripts/eval_generated_harness.py`, `tests/test_generated_harness_contract.py` | `python -m unittest tests.test_generated_harness_contract -q` | The evaluator fails security audit, legal research, financial analysis, hiring, and customer support harness mutations that omit key safety, boundary, privacy, source, escalation, or bias guardrails. |
| Eval scores and gates can be tracked over time. | `scripts/record_eval_snapshot.py`, `Docs/Environment/EVAL_TRENDS.md`, `Docs/Environment/eval-history/` | `python scripts/record_eval_snapshot.py` | Maintainers can archive compact eval snapshots and review pass/fail trends without depending on CI log retention. |
| Official OpenAI source citations have a freshness check. | `scripts/check_source_freshness.py`, `Docs/Environment/SOURCE_FRESHNESS.md`, `Docs/Environment/SOURCE_FRESHNESS.json` | `python scripts/check_source_freshness.py` | Maintainers can verify cited `developers.openai.com` docs are reachable and trigger semantic review when official sources move or disappear. |
| Core local guidance has a semantic drift signal. | `scripts/check_semantic_alignment.py`, `Docs/Environment/SEMANTIC_ALIGNMENT.md`, `Docs/Environment/SEMANTIC_ALIGNMENT.json` | `python scripts/check_semantic_alignment.py` | Maintainers can verify that local guidance and official OpenAI docs still share the named concepts this repo depends on: AGENTS.md loading, permissions, subagents, skills, and config model controls. |
| Real usage evidence has a privacy-checked capture path. | `scripts/record_usage_case.py`, `scripts/validate_usage_records.py`, `Docs/Environment/USAGE_RECORDS.md`, `tests/test_record_usage_case.py`, `tests/test_validate_usage_records.py` | `python scripts/validate_usage_records.py && python -m unittest tests.test_record_usage_case tests.test_validate_usage_records -q` | Maintainers can record and validate synthetic, sanitized, or private-summary usage records while rejecting obvious secrets, credentials, email addresses, local machine paths, malformed JSON, filename/slug mismatches, weak non-synthetic records, invalid source/generation metadata, and empty required fields. |
| Copied harnesses can export public-safe evidence packets. | `scripts/export_evidence_packet.py`, `scripts/codex_harness.py`, `tests/test_export_evidence_packet.py`, `tests/test_codex_harness_cli.py` | `tmp=$(mktemp -d) && cp -R tests/fixtures/generated_harnesses/software-dev-basic "$tmp/harness" && python "$tmp/harness/scripts/record-task-trial.py" --task "demo" --outcome success --evidence "public-safe artifact" --verification "python scripts/run-harness-evals.py" --privacy-review "synthetic public-safe test only" --limitations "one copied-harness smoke" && python scripts/codex_harness.py evidence-packet "$tmp/harness" --out "$tmp/HARNESS_EVIDENCE_PACKET.md" --harness-label "synthetic copied harness" --min-successes 1 --json` | Users can export a Markdown packet summarizing copied-harness eval status, task-trial outcomes, recent complete trials, improvement-backlog counts, privacy boundaries, and issues before maintainers decide whether to create a checked-in usage record. |
| External pilots have a one-task evidence kit. | `scripts/prepare_pilot.py`, `scripts/prepare_next_pilot.py`, `scripts/prepare_pilot_batch.py`, `scripts/export_pilot_pack.py`, `scripts/codex_harness.py`, `tests/test_prepare_pilot.py`, `tests/test_prepare_next_pilot.py`, `tests/test_prepare_pilot_batch.py`, `tests/test_external_pilot_pack.py`, `tests/test_codex_harness_cli.py` | `python scripts/codex_harness.py prepare-pilot /tmp/codex-rag-pilot --brief "RAG app with prompts, evals, and retrieval checks" --domain "LLM app" --slug rag-pilot --title "RAG pilot" --pilot-record-dir Docs/Environment/pilot-records --force --json && python -m unittest tests.test_prepare_pilot tests.test_prepare_next_pilot tests.test_prepare_pilot_batch tests.test_external_pilot_pack tests.test_codex_harness_cli -q` | Maintainers can prepare a generated pilot harness, public-safe pilot guide, GitHub issue-body draft, and optional pilot-board record in one command before the reporter runs a real task; they can also use `codex-harness prepare-next-pilot` to choose the next suggested pilot directly from current usage gaps or `codex-harness prepare-pilot-batch` to dry-run or prepare the suggested beta-exit pilot set under one target root. Copied-harness pilot packs can also prefill that draft from the latest complete task-trial record plus local eval status so reporters still know the one-task loop, evidence commands, privacy boundary, maintainer conversion commands, and claim limits before submitting external usage evidence. |
| External pilot campaigns can be planned from current gaps. | `scripts/export_pilot_campaign.py`, `scripts/usage_gaps.py`, `scripts/prepare_pilot.py`, `scripts/prepare_next_pilot.py`, `Docs/Environment/PILOT_CAMPAIGN.md`, `tests/test_pilot_campaign.py`, `tests/test_prepare_pilot.py`, `tests/test_prepare_next_pilot.py`, `tests/test_codex_harness_cli.py` | `python scripts/codex_harness.py pilot-campaign --no-write --json && python -m unittest tests.test_pilot_campaign tests.test_prepare_pilot tests.test_prepare_next_pilot -q` | Maintainers can turn current usage gaps into a shareable pilot campaign with profile-targeted slots, source type, generation path, commands, reporter checklist, maintainer follow-up, claim boundaries, and a listed-pilot coverage projection, then use `codex-harness prepare-next-pilot` or `codex-harness prepare-pilot` to create the next pilot harness and evidence kit without pretending preparation itself is adoption proof. |
| Prepared pilots have a tracked funnel before usage conversion. | `scripts/pilot_board.py`, `scripts/prepare_next_pilot.py`, `Docs/Environment/PILOT_BOARD.md`, `Docs/Environment/pilot-records/`, `tests/test_pilot_board.py`, `tests/test_prepare_next_pilot.py`, `tests/test_codex_harness_cli.py` | `python scripts/codex_harness.py pilot-board --no-write --json && python -m unittest tests.test_pilot_board tests.test_prepare_next_pilot tests.test_codex_harness_cli -q` | Maintainers can write optional prepared-pilot records from `codex-harness prepare-next-pilot --pilot-record-dir ...`, inspect and update pending, invited, completed, converted, and dropped pilots with `codex-harness pilot-board` and `codex-harness pilot-update`, reject local machine paths in checked-in public target metadata, and keep the claim boundary explicit: pilot-board records track outreach state only and do not become usage proof until converted into validated usage records whose domain, source type, and generation path match the pilot. |
| Active pilots have reporter-ready outreach packets, handoff folders, and GitHub issue drafts. | `scripts/export_pilot_outreach.py`, `scripts/export_pilot_handoff.py`, `scripts/audit_pilot_handoffs.py`, `scripts/export_pilot_github_issues.py`, `scripts/usage_from_github_issue.py`, `scripts/pilot_board.py`, `Docs/Environment/PILOT_OUTREACH.md`, `Docs/Environment/PILOT_HANDOFF_AUDIT.md`, `Docs/Environment/PILOT_GITHUB_ISSUES.md`, `Docs/Environment/pilot-handoffs/`, `Docs/Environment/pilot-github-issues/`, `tests/test_pilot_outreach.py`, `tests/test_pilot_handoff.py`, `tests/test_pilot_handoff_audit.py`, `tests/test_pilot_github_issues.py`, `tests/test_usage_from_github_issue.py`, `tests/test_codex_harness_cli.py` | `python scripts/codex_harness.py pilot-outreach --no-write --json && python scripts/codex_harness.py pilot-handoff --out Docs/Environment/pilot-handoffs --force --json && python scripts/codex_harness.py pilot-handoff-audit --handoff-dir Docs/Environment/pilot-handoffs --json && python scripts/codex_harness.py pilot-github-issues --no-write --json && python -m unittest tests.test_pilot_outreach tests.test_pilot_handoff tests.test_pilot_handoff_audit tests.test_pilot_github_issues tests.test_usage_from_github_issue tests.test_codex_harness_cli -q` | Maintainers can turn prepared, invited, or completed pilot records into a public-safe outreach packet, audited shareable per-pilot folder, or GitHub-ready issue queue with a single `REPORTER_HANDOFF.md`, reporter message, pilot-pack and issue-draft materials, a prefilled `USAGE_REPORT_DRAFT.md`, public issue bodies, `gh issue create` commands, status-update commands, issue-body conversion commands, live GitHub issue conversion commands, copied-harness conversion commands, required-file and claim-boundary checks, and a claim boundary that invite tracking, issue opening, handoff creation, or handoff auditing is not adoption evidence. |
| Copied harness evidence can become usage proof. | `scripts/usage_from_harness.py`, `scripts/codex_harness.py`, `tests/test_usage_from_harness.py`, `tests/test_codex_harness_cli.py` | `tmp=$(mktemp -d) && cp -R tests/fixtures/generated_harnesses/software-dev-basic "$tmp/harness" && python "$tmp/harness/scripts/record-task-trial.py" --task "demo" --outcome success --evidence "public-safe artifact" --verification "python scripts/run-harness-evals.py" --privacy-review "synthetic public-safe test only" --limitations "one copied-harness smoke" && python scripts/codex_harness.py local-eval "$tmp/harness" && python scripts/codex_harness.py usage-from-harness "$tmp/harness" --slug demo --title "Demo" --domain "software development" --evidence-type synthetic --privacy-review "synthetic public-safe test only" --record-dir "$tmp/records" --report "$tmp/USAGE_RECORDS.md" --json` | A generated harness's local eval report and task-trial records can be converted into a privacy-checked usage record without publishing raw logs, secrets, personal data, or private repository paths. |
| External users have a privacy-safe evidence intake path. | `.github/ISSUE_TEMPLATE/external-usage-report.yml`, `Docs/Environment/EXTERNAL_USAGE_EVIDENCE.md`, `Docs/Environment/EXTERNAL_USAGE_ISSUE_EXAMPLE.md`, `scripts/usage_from_issue.py`, `scripts/check_cli_install.py`, `tests/test_external_usage_evidence.py`, `tests/test_usage_from_issue.py`, `tests/test_cli_install.py` | `python -m unittest tests.test_external_usage_evidence tests.test_usage_from_issue tests.test_cli_install -q` | Public reporters are guided to share pilot or usage-record slug, domain, harness label, outcome, evidence, verification, privacy review, and limitations without secrets, private paths, personal data, proprietary source, raw logs, or raw transcripts; maintainers can lint issue-form bodies with `--lint-only` to find missing fields, weak evidence counts, and privacy problems, then preview with `--no-write` before converting them into checked usage records with the same privacy guardrails. A checked synthetic issue example is linted and previewed as importer proof, not usage proof. The importer infers slug from complete issue bodies, including stdin, while still allowing explicit `--slug` for older reports. The installed CLI smoke verifies lint, preview, write, and linked pilot conversion; when the issue matches a prepared pilot slug, `usage-from-issue --pilot-record-dir` marks the pilot converted, validates the usage-record reference, and refreshes the pilot board. |
| Non-synthetic product-proof claims have explicit thresholds. | `scripts/validate_usage_records.py`, `scripts/proof_status.py`, `Docs/Environment/USAGE_RECORDS.md`, `Docs/Environment/usage-records/`, `tests/test_proof_status.py` | `python scripts/validate_usage_records.py --min-records 2 --require-non-synthetic --require-success && python -m unittest tests.test_proof_status -q` | Maintainers can distinguish "the usage-record lane is healthy" from "beta-exit evidence exists"; the current checked-in evidence proves two sanitized self-dogfood successes, not broad external adoption, multi-domain coverage, or installed brief-based generation adoption through `prepare-next-pilot`, `prepare-pilot`, `quickstart`, or `init --brief`. |
| Beta-exit usage gaps are inspectable. | `scripts/usage_gaps.py`, `scripts/codex_harness.py`, `tests/test_usage_gaps.py`, `Docs/Environment/USAGE_GAPS.md` | `python scripts/codex_harness.py usage-gaps --no-write --json && python -m unittest tests.test_usage_gaps -q` | Maintainers can see remaining record, external/multi-project, domain, and installed brief-based generation gaps without conflating valid-record health with beta-exit readiness, then inspect whether the suggested pilot set would close those thresholds if completed and converted into real usage evidence. |
| Next proof actions are packaged from current gaps. | `scripts/proof_next.py`, `scripts/codex_harness.py`, `Docs/Environment/PROOF_NEXT.md`, `tests/test_proof_next.py`, `tests/test_codex_harness_cli.py` | `python scripts/codex_harness.py proof-next --no-write --json && python -m unittest tests.test_proof_next tests.test_codex_harness_cli -q` | Maintainers can turn the current beta-exit gap into one operator packet with the next pilot, candidate coverage projection, batch dry-run command, single-pilot prepare command, pilot-board check, outreach and handoff export commands, handoff audit command, usage conversion command, beta-exit audit command, final proof-status command, and claim boundary, without treating the packet itself as external adoption evidence. |
| Beta-exit readiness has a non-gating audit. | `scripts/beta_exit_audit.py`, `Docs/Environment/BETA_EXIT_AUDIT.md`, `tests/test_beta_exit_audit.py`, `tests/test_codex_harness_cli.py` | `python scripts/codex_harness.py beta-exit-audit --no-write --json && python -m unittest tests.test_beta_exit_audit tests.test_codex_harness_cli -q` | Maintainers can publish a single beta-exit audit that names missing usage thresholds, source-check state, pilot-board state, final gate commands, and next actions without treating "audit generated" as proof that external adoption exists. |
| Product-proof readiness has a single status report. | `scripts/proof_status.py`, `scripts/refresh_generated_surfaces.py`, `Docs/Environment/PROOF_STATUS.md`, `Docs/Environment/CODEX_EQUIVALENCE_MATRIX.md`, `examples/live-create/TASK_TRIALS.md`, `Docs/Environment/USAGE_RECORDS.md`, `Docs/Environment/USAGE_GAPS.md`, `Docs/Environment/PILOT_CAMPAIGN.md`, `Docs/Environment/PILOT_BOARD.md`, `Docs/Environment/PILOT_OUTREACH.md`, `Docs/Environment/PILOT_HANDOFF_AUDIT.md`, `Docs/Environment/PROOF_NEXT.md`, `Docs/Environment/BETA_EXIT_AUDIT.md` | `python scripts/codex_harness.py refresh-examples --json && python scripts/proof_status.py` | Maintainers can refresh checked-in generated examples and fixtures from the current generator, then summarize checked-in proof status, equivalence mapping, task-trial coverage, non-synthetic usage evidence, usage gaps, pilot-campaign readiness, pilot-board visibility, pilot-outreach and handoff-audit readiness, next proof actions, and beta-exit audit availability in one calibrated report that also lists what the evidence does not prove. |
| Local checkout readiness is inspectable before release work. | `scripts/doctor.py`, `scripts/codex_harness.py`, `tests/test_doctor.py`, `tests/test_codex_harness_cli.py` | `python scripts/codex_harness.py doctor` | Users can quickly verify Python version, required public files, supported profiles, checked-in example inventory, usage-record threshold, and proof-status report before generating a harness or running the heavier gate. |
| Beta exit criteria are explicit. | `Docs/Environment/ROADMAP.md`, `README.md`, `CONTRIBUTING.md`, `tests/test_project_support_files.py` | `python -m unittest tests.test_project_support_files -q` | The repo names the evidence threshold required before dropping the beta label, including external usage, multi-domain coverage, installed brief-based generation usage, source-freshness, and semantic-alignment checks. |
| Checked-in generated examples cannot silently drift from supported profiles. | `scripts/check_example_inventory.py`, `scripts/refresh_generated_surfaces.py`, `tests/test_example_inventory.py`, `tests/test_refresh_generated_surfaces.py`, `examples/deterministic/`, `examples/create-acceptance/`, `examples/brief-acceptance/` | `python scripts/codex_harness.py refresh-examples --json && python scripts/check_example_inventory.py` | The release gate fails if deterministic, create-acceptance, brief-acceptance, or fixture examples are missing, partial, duplicated, or out of sync with the expected profile, generated task-trial recorder, and generated public-report exporter contracts; maintainers also have one command to refresh those surfaces before checking. |
| Generated harnesses have one-command local validation. | `scripts/validate_generated_harness.py`, `scripts/codex_harness.py`, `tests/test_validate_generated_harness.py`, `tests/test_codex_harness_cli.py` | `python scripts/codex_harness.py validate tests/fixtures/generated_harnesses/software-dev-basic && python scripts/codex_harness.py local-eval tests/fixtures/generated_harnesses/software-dev-basic --json --no-write` | Users can run the contract evaluator, offline smoke resolver, generated local self-check, and copied-harness eval report together before trusting or publishing a generated harness. |
| Legacy harness migration work is inspectable, plannable, and packetized. | `scripts/migration_audit.py`, `scripts/prepare_migration.py`, `scripts/codex_harness.py`, `tests/fixtures/legacy_harnesses/legacy-basic/`, `tests/test_migration_audit.py`, `tests/test_prepare_migration.py`, `tests/test_codex_harness_cli.py` | `tmp=$(mktemp -d) && python scripts/codex_harness.py prepare-migration tests/fixtures/legacy_harnesses/legacy-basic "$tmp/migration-packet" --source-label "legacy fixture" --json` | Users can identify legacy instruction, settings, agent, command, hook, skill, ignore, tool-name, and provider-variable assumptions from a checked-in legacy-shaped fixture, then prepare one local migration packet with audit report, Codex blueprint, adoption plan, add-only copy script, validation/manual-review flow, claim boundary, and cleanup paths to archive or remove only after translation. |
| The public helper command installs and runs. | `pyproject.toml`, `scripts/codex_harness.py`, `scripts/check_cli_install.py`, `tests/test_cli_install.py`, `.github/workflows/evals.yml` | `python scripts/check_cli_install.py` | A temporary non-editable install exposes `codex-harness`, lists 20 profiles, runs `codex-harness doctor`, runs `codex-harness init --brief`, runs `codex-harness quickstart`, runs `codex-harness init --from-project`, runs `codex-harness demo-capture`, validates and inspects the generated harness, writes an adoption plan, checks equivalence, smokes upstream-drift self-compare, runs its copied local eval report, exports a generated public usage report draft, exports an evidence packet, converts copied-harness evidence, lints issue-form evidence, previews issue-form evidence, writes issue-form evidence into usage records, prepares and updates a pilot record, dry-runs the suggested pilot batch, exports and audits a pilot handoff, converts a matching issue into a validated pilot-board conversion, reports usage gaps, writes a pilot campaign, writes proof-next, writes a migration plan report, migration-audits it, prepares a migration packet, and evaluates it directly. |
| The standard release gate is CI-safe and offline. | `scripts/run_evals.py`, `.github/workflows/evals.yml` | `python scripts/run_evals.py` | Static port checks, fixture evals, offline smokes, deterministic generation, checked-in example inventory, checked-in deterministic/create-acceptance/brief-acceptance/demo-capture/live-create examples, unit/mutation tests, and compile checks pass without authenticated live services. |
| Authenticated local live smoke works through Codex CLI. | `scripts/smoke_generated_harness.py`, `scripts/run_evals.py --codex-live` | `python scripts/run_evals.py --codex-live` | On a machine with authenticated Codex CLI, checked-in create-acceptance examples can be loaded through non-interactive `codex exec`. |

## Full Local Gate

Run before release or public claims:

```bash
python scripts/run_evals.py
```

Run on an authenticated maintainer machine when live Codex CLI coverage is
available:

```bash
python scripts/run_evals.py --codex-live
```

To live-smoke every checked-in create-acceptance profile:

```bash
python scripts/run_evals.py --codex-live --codex-live-profile all
```

## What This Does Not Prove

- It does not prove that every live model-mediated `/create` interview will
  produce an ideal custom harness on the first attempt.
- It does not prove domain-specific quality for every bundled domain preset.
- It does not prove organization-level compliance, policy enforcement, or
  production security controls.
- It does not prove future Codex docs or config schema compatibility after docs
  drift; that is why static source checks and scheduled evals remain required.

## Remaining Product-Proof Work

- Add more live examples for specialized or higher-risk domains as new public-safe
  synthetic briefs become available.
- Add more non-synthetic real-world usage records using the privacy-checked
  recorder, especially external or multi-project records beyond the current
  sanitized self-dogfood evidence.
- Deepen semantic drift checks beyond concept presence when stable
  machine-readable official-doc metadata is available.
