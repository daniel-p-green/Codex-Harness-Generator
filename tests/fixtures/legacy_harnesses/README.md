# Legacy Harness Fixtures

These fixtures intentionally contain older harness paths and terminology so
`scripts/migration_audit.py` can be regression-tested against realistic
migration inputs.

They are not Codex-native generated harnesses and should not be copied into
examples or release artifacts.
