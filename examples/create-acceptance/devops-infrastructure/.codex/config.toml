model = "gpt-5.5"
model_reasoning_effort = "medium"
model_verbosity = "medium"
approval_policy = "on-request"
default_permissions = "devops-infra"

[agents]
max_threads = 4
max_depth = 1

[agents.reviewer]
description = "Reviews infrastructure changes for blast radius, rollback paths, secret handling, and missing validation."
config_file = "agents/reviewer.toml"

[[skills.config]]
path = "../.agents/skills/health-check"
enabled = true

[permissions.devops-infra]
description = "Workspace write access with sensitive files denied."
extends = ":workspace"

[permissions.devops-infra.filesystem]
glob_scan_max_depth = 4

[permissions.devops-infra.filesystem.":workspace_roots"]
"." = "write"
"**/.env" = "deny"
"**/.env.*" = "deny"
"**/*secret*" = "deny"
"**/*token*" = "deny"
"**/*credential*" = "deny"
"**/*.pem" = "deny"
"**/*.key" = "deny"

[permissions.devops-infra.network]
enabled = true
mode = "limited"

[permissions.devops-infra.network.domains]
"developers.openai.com" = "allow"
"docs.github.com" = "allow"
