# Legacy Harness Instructions

This legacy fixture intentionally uses older harness conventions so migration
tests can verify the Codex migration audit. It is not a Codex-native harness.

- Use WebSearch for outside sources.
- Keep durable project instructions here until migration moves them to AGENTS.md.
